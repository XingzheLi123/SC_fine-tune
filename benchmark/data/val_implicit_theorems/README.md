# val_implicit_theorems

Theorem-implicit mirror of `benchmark/data/val`. Problems, parameters, canonical answers, and surface perturbations are preserved. Reasoning traces use the same mathematical steps but omit explicit theorem-bank statements.

Record count: 240.

Use this split with `train_implicit_theorems/` to validate the theorem-implicit fine-tuning ablation.
